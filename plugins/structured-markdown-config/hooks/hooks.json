{
  "_comment": "Structured Markdown Config Plugin - Comprehensive validation for all markdown configuration files",
  "_notes": [
    "This plugin merges enhanced-rules and output-styles-permission-modes",
    "Validates agents, skills, rules, and CLAUDE.md files",
    "Enforces tool restrictions from output styles",
    "Hooks are shared from ${CLAUDE_PROJECT_DIR}/shared/hooks/"
  ],
  "PreToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/enforce-structured-markdown.ts",
          "matcher": "Write|Edit"
        },
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/enforce-output-style-tools.ts"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/run-rule-checks.ts",
          "matcher": "Write|Edit"
        }
      ]
    }
  ]
}
