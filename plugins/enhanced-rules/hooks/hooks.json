{
  "_comment": "Enhanced Rules Plugin - Context-aware rules and constraints",
  "_notes": [
    "This plugin provides rule enforcement through custom checks and markdown heading validation.",
    "Hooks: PreToolUse (heading validation), PostToolUse (rule checks)",
    "Rule hooks are shared from ${CLAUDE_PROJECT_DIR}/shared/hooks/"
  ],
  "PreToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/enforce-rule-md-headings.ts",
          "matcher": "Write"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/run-rule-checks.ts",
          "matcher": "Write|Edit"
        }
      ]
    }
  ]
}
