{
  "_comment": "Enhanced Rules Plugin - Context-aware rules and constraints",
  "_notes": [
    "This plugin provides rule enforcement through custom checks and markdown heading validation.",
    "Hooks: PreToolUse (heading validation), PostToolUse (rule checks)",
    "Rule hooks are shared from ${CLAUDE_PROJECT_DIR}/shared/rules/"
  ],
  "PreToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "node ${CLAUDE_PROJECT_DIR}/shared/runner.ts ${CLAUDE_PROJECT_DIR}/shared/rules/enforce-rule-md-headings.ts",
          "matcher": "Write"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "node ${CLAUDE_PROJECT_DIR}/shared/runner.ts ${CLAUDE_PROJECT_DIR}/shared/rules/run-rule-checks.ts",
          "matcher": "Write|Edit"
        }
      ]
    }
  ]
}
