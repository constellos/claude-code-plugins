{
  "_comment": "Markdown Rules Plugin - Markdown heading validation and rule enforcement",
  "_notes": [
    "This plugin enforces markdown heading structure and runs custom rule checks.",
    "Hooks: PreToolUse (heading validation for Write|Edit), PostToolUse (rule checks for Write|Edit)",
    "Rule hooks are shared from ${CLAUDE_PROJECT_DIR}/shared/hooks/"
  ],
  "PreToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/enforce-markdown-rules.ts",
          "matcher": "Write|Edit"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PROJECT_DIR}/shared/hooks/run-rule-checks.ts",
          "matcher": "Write|Edit"
        }
      ]
    }
  ]
}
