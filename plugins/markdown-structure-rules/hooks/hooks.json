{
  "_comment": "Markdown Structure Rules Plugin - Validates rules files in .claude/rules/",
  "_notes": [
    "Validates only rules files in .claude/rules/",
    "Requires 'Required Skills' frontmatter field",
    "Requires '## Rules' heading"
  ],
  "description": "Markdown structure validation for rules files in .claude/rules/",
  "hooks": {
    "PreToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "command",
          "command": "npx tsx ${CLAUDE_PLUGIN_ROOT}/hooks/validate-rules-structure.ts",
          "description": "Validates rules file structure in .claude/rules/. Ensures 'Required Skills' frontmatter and '## Rules' heading are present."
        }
      ]
    }
  ]
  }
}
