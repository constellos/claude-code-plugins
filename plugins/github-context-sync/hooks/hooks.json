{
  "_comment": "GitHub Context Sync Plugin - GitHub state synchronization",
  "_notes": [
    "Displays branch issue context and sync status",
    "Encourages UI review after ui-developer agent"
  ],
  "description": "GitHub context and state synchronization",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx tsx ${CLAUDE_PLUGIN_ROOT}/hooks/fetch-branch-context.ts",
            "description": "Displays branch issue context, sync status, and outstanding issues"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsx ${CLAUDE_PLUGIN_ROOT}/hooks/encourage-ui-review.ts",
            "description": "Encourages ui-reviewer after ui-developer agent completes"
          }
        ]
      }
    ]
  }
}
